name: utto-test

services:
  db-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: utto_test
      POSTGRES_USER: utto_test
      POSTGRES_PASSWORD: local-test-only
    ports:
      - "127.0.0.1:55432:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U utto_test -d utto_test"]
      interval: 2s
      timeout: 2s
      retries: 15
